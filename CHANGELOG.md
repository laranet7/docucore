# Changelog

## 0.1.0 - Initial public-ready baseline

### Added

- CLI commands: `init`, `scan`, `build`, `snapshot`, and `status`
- Generic scanner
- Plugin architecture
- Initial plugins for generic, Python, Node, .NET, and Docker detection
- Markdown outputs
- Snapshot and `latest` documentation workflow
- External project mode with `--project` and `--output`
- CI workflow for tests and lint
